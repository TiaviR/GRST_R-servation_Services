<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GRST_Services" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/classy_dark</item>
        <item name="colorPrimaryVariant">@color/classy_dark</item>
        <item name="colorOnPrimary">@color/classy_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Texte par défault -->
    <style name="DefaultTextStyle">
        <item name="android:textColor">@color/classy_dark</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/kanit</item>
    </style>

    <!-- Texte Sous-titre par défault -->
    <style name="DefaultSubtitle" parent="DefaultTextStyle">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Texte Titre par défault -->
    <style name="DefaultTitle" parent="DefaultTextStyle">
        <item name="android:textSize">24sp</item>
    </style>

    <!-- Texte Sous-sous-titre par défault -->
    <style name="DefaultSubSubtitle" parent="DefaultTextStyle">
        <item name="android:textColor">#7E7E7D</item>
    </style>

    <!-- Style pour la bottom navigation view -->
    <style name="BottomNavigationStyle" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/kanit</item>
    </style>
    
    <!-- Personnaliser la barre de navigation -->
    <style name="BottomNavigationTheme" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/kanit</item>
    </style>
</resources>